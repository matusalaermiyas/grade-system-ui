function MainTitle({ title }) {
  return (
    <div className="col s8 push-s1">
      <h1 style={{ fontSize: 25, fontWeight: 800, color: "grey" }}>{title}</h1>
    </div>
  );
}

export default MainTitle;
